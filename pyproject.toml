[project]
name = "m-agent"
version = "0.1.0"
description = "An embeddable Agent Application Runtime for Python."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
