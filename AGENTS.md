## Agent skills

### Issue tracker

本仓库使用 `.scratch/<feature-slug>/` 下的本地 Markdown 文件管理 Issue 和 PRD；外部 PR 不属于分诊入口。详见 `docs/agents/issue-tracker.md`。

### Triage labels

分诊使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix` 五个标准状态名。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 single-context 布局。探索代码前应读取根目录的 `CONTEXT.md` 和 `docs/adr/` 下相关的架构决策。详见 `docs/agents/domain.md`。
